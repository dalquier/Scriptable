const app=document.getElementById('app');
const state={view:'today',conversations:[],current:null,messages:[],busy:false,error:null,failedUserMessageId:null,deleteTarget:null,provider:'',model:''};
async function pyto(action,payload={}){const r=await fetch('/api',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,payload})});const d=await r.json();if(!r.ok)throw new Error(d.message||'Erreur');handle(d);return d}
function handle(d){state.busy=false;if(d.type==='initial_state'){state.conversations=d.conversations||[];state.provider=d.provider||'';state.model=d.model||''}else if(d.type==='conversation_created'){state.current=d.conversation;state.messages=[];state.view='conversation'}else if(d.type==='conversation_loaded'){state.current=d.conversation;state.messages=d.messages||[];state.view='conversation'}else if(d.type==='message_result'){merge(d.user_message);merge(d.assistant_message);state.current=d.conversation;if(d.provider_error){state.error=d.error_message||'Réponse impossible.';state.failedUserMessageId=d.user_message?.id}else{state.error=null;state.failedUserMessageId=null;clearDraft()}pyto('list_conversations')}else if(d.type==='conversation_deleted'){state.conversations=state.conversations.filter(c=>c.id!==d.conversation_id);state.view='history';state.deleteTarget=null}else if(d.type==='error'){state.error=d.message||'Erreur'}render()}
function merge(m){if(!m)return;const i=state.messages.findIndex(x=>x.id===m.id);if(i>=0)state.messages[i]=m;else state.messages.push(m);state.messages.sort((a,b)=>a.sequence-b.sequence)}
function render(){if(state.view==='conversation')return renderConversation();app.innerHTML=`<div class="shell"><main class="page">${state.error?errorHtml(state.error):''}${state.view==='history'?historyHtml():todayHtml()}</main><nav class="nav"><button class="${state.view==='today'?'active':''}" onclick="go('today')">Aujourd’hui</button><button class="${state.view==='history'?'active':''}" onclick="go('history')">Historique</button></nav>${modalHtml()}</div>`}
function todayHtml(){const c=state.conversations[0];return `<section class="hero"><div class="eyebrow">Aujourd’hui</div><h1>Comment puis-je vous aider&nbsp;?</h1><p>Un espace simple pour clarifier ce qui se passe et choisir le prochain pas utile.</p><div class="status-pill"><span class="status-dot"></span>${esc(state.provider)} · ${esc(state.model)}</div></section><section class="quick-grid"><button class="quick-card" onclick="startWith('J’ai besoin de parler d’une situation qui me préoccupe.')"><div class="quick-icon">💬</div><div class="quick-title">Parler maintenant</div><div class="quick-subtitle">Déposer une situation et y voir plus clair.</div></button><button class="quick-card" onclick="startWith('Je voudrais faire un bilan rapide de mon état aujourd’hui.')"><div class="quick-icon">🌿</div><div class="quick-title">Faire un bilan</div><div class="quick-subtitle">Énergie, humeur, tension et priorités du jour.</div></button><button class="quick-card" onclick="startWith('Je dois prendre une décision et j’aimerais la préparer.')"><div class="quick-icon">🧭</div><div class="quick-title">Préparer une décision</div><div class="quick-subtitle">Distinguer faits, options et conséquences.</div></button><button class="quick-card" onclick="startWith('Je voudrais comprendre une pensée ou une émotion récurrente.')"><div class="quick-icon">🧠</div><div class="quick-title">Comprendre un schéma</div><div class="quick-subtitle">Explorer une pensée, une émotion ou un déclencheur.</div></button></section><section class="section"><div class="section-head"><h2>Continuer</h2><button class="link-button" onclick="go('history')">Tout voir</button></div>${c?`<article class="resume-card"><h3>${esc(c.title)}</h3><p>${esc(c.last_message_preview||'Aucun message')}</p><button class="primary" onclick="openConversation('${c.id}')">Reprendre la conversation</button></article>`:'<div class="empty">Aucune conversation pour le moment.</div>'}<button class="primary" onclick="createConversation()">Nouvelle conversation</button></section>`}
function historyHtml(){return `<section class="hero"><div class="eyebrow">Historique</div><h1>Vos conversations</h1><p>Retrouvez, reprenez ou supprimez les échanges conservés localement sur cet appareil.</p></section><section class="history-list">${state.conversations.length?state.conversations.map(c=>`<article class="history-card"><button class="history-open" onclick="openConversation('${c.id}')"><strong>${esc(c.title)}</strong><div class="meta">${c.message_count} message${c.message_count>1?'s':''}</div><div class="preview">${esc(c.last_message_preview||'')}</div></button><button class="danger-link" onclick="askDelete('${c.id}')">Supprimer</button></article>`).join(''):'<div class="empty">Aucune conversation.</div>'}</section>`}
function renderConversation(){app.innerHTML=`<div class="shell conversation-shell"><header class="conversation-top"><button class="back" onclick="leaveConversation()">‹ Retour</button><div class="conversation-title">${esc(state.current?.title||'Conversation')}</div></header><main class="conversation-content" id="messageList"><div class="messages">${state.messages.length?state.messages.map(m=>`<div class="bubble ${m.role}">${esc(m.content)}</div>`).join(''):'<div class="empty">Écrivez librement ce qui vous préoccupe.</div>'}${state.busy?'<div class="loading">Réponse en préparation…</div>':''}${state.error?errorHtml(state.error,state.failedUserMessageId?'<button class="link-button" onclick="retryResponse()">Réessayer</button>':''):''}</div></main><footer class="composer"><div class="composer-inner"><textarea id="composer" rows="1" placeholder="Écrivez ce qui vous préoccupe…" oninput="draft(this)"></textarea><button onclick="sendMessage()" ${state.busy?'disabled':''}>Envoyer</button></div></footer></div>`;requestAnimationFrame(()=>{const e=document.getElementById('composer');if(e){e.value=readDraft();resize(e)}const l=document.getElementById('messageList');if(l)l.scrollTop=l.scrollHeight})}
function errorHtml(t,a=''){return `<div class="error"><strong>Un problème est survenu</strong><div>${esc(t)}</div>${a}</div>`}
function modalHtml(){if(!state.deleteTarget)return'';return `<div class="modal-backdrop"><div class="modal"><h2>Supprimer cette conversation&nbsp;?</h2><p>Cette action supprime aussi tous ses messages.</p><div class="modal-actions"><button onclick="cancelDelete()">Annuler</button><button class="danger" onclick="confirmDelete()">Supprimer</button></div></div></div>`}
function go(v){state.view=v;state.error=null;pyto('list_conversations');render()}function leaveConversation(){state.view='today';pyto('list_conversations');render()}function createConversation(){if(state.busy)return;state.busy=true;pyto('create_conversation');render()}async function startWith(text){if(state.busy)return;state.busy=true;const d=await pyto('create_conversation');state.current=d.conversation;state.messages=[];state.view='conversation';render();requestAnimationFrame(()=>{const e=document.getElementById('composer');if(e){e.value=text;resize(e);e.focus()}})}function openConversation(id){if(state.busy)return;state.busy=true;pyto('open_conversation',{conversation_id:id});render()}function sendMessage(){const e=document.getElementById('composer');const text=(e?.value||'').trim();if(!text||state.busy||!state.current)return;state.busy=true;state.error=null;pyto('send_message',{conversation_id:state.current.id,text,request_id:crypto.randomUUID?crypto.randomUUID():`${Date.now()}-${Math.random()}`});render()}function retryResponse(){if(!state.failedUserMessageId)return;state.busy=true;pyto('retry_response',{conversation_id:state.current.id,user_message_id:state.failedUserMessageId});render()}function askDelete(id){state.deleteTarget=id;render()}function cancelDelete(){state.deleteTarget=null;render()}function confirmDelete(){state.busy=true;pyto('delete_conversation',{conversation_id:state.deleteTarget});render()}function draft(e){localStorage.setItem(`draft-${state.current.id}`,e.value);resize(e)}function readDraft(){return state.current?localStorage.getItem(`draft-${state.current.id}`)||'':''}function clearDraft(){if(state.current)localStorage.removeItem(`draft-${state.current.id}`)}function resize(e){e.style.height='auto';e.style.height=`${Math.min(e.scrollHeight,126)}px`}function esc(v=''){return String(v).replace(/[&<>'"]/g,s=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[s]))}
if(window.visualViewport){const u=()=>document.documentElement.style.setProperty('--vh',`${window.visualViewport.height}px`);visualViewport.addEventListener('resize',u);u()}render();setTimeout(()=>pyto('app_ready'),0);
