# Contributing

Use Python 3.11+, create a focused branch, add tests for behavior changes, and run `python scripts/validate_build1.py` before opening a pull request. Public APIs require type annotations and docstrings. Keep the kernel free of business logic and dependencies minimal.
