# Testing

`pytest` covers imports, bootstrap, lifecycle, idempotence, failure paths, container behavior, configuration precedence, validation, health, diagnostics, CLI and an end-to-end flow. `pytest-cov` enforces 85% branch-aware coverage for `developeros.core`. Ruff, Mypy strict mode and Python compilation are mandatory.
